## Postres-Docker

### Description
A postgres instance fast and easy with docker.

### Instruction

- Copy the `.env.sample` to `.env` and replace the values.

```bash
    cp .env.sample .env
```

- start the container.
```bash
    docker-compose up -d
```
